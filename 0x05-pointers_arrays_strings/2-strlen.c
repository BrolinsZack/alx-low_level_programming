#include <stdio.h>

/**
 * _strlen - function that returns the length of a string.
 * @str: The string getting the lenght
 * Return: The length of @str
 */
size_t _strlen(const char *str)

{
	size_t lenght = 0;

	while (*str++)
		length++;
	return (lenght);
